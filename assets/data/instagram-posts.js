/* =============================================================
   Instagram — the most recent posts. GENERATED. Do not hand-edit.

   tools/fetch-instagram.py writes this from Instagram's own API. It
   is checked in empty so the site works before any of that is set up:
   with nothing here, assets/js/instagram.js falls back to whatever is
   pinned by hand in assets/data/instagram.js.

   Only permalinks and dates are kept. The image addresses the API
   returns are signed and expire within days, so storing them would
   leave a grid of broken pictures; the pictures come from Instagram's
   own embed when someone looks.
   ============================================================= */
window.BIOSOC_INSTAGRAM_POSTS = {
  "fetchedAt": null,
  "posts": []
};
